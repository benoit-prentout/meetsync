node_modules
dist
.DS_Store
*.zip
.vscode
.playwright-mcp
.superpowers
# src/lib/backendChecksum.ts — committed for tsc type-check; regenerated by Vite plugin on each build
# src/lib/bundledBackend.ts — same
